#!/usr/bin/env bash

set -ex

if [[ $1 =~ ruamel ]]; then
  cd /work/ruamel/
  pip install --install-option="--prefix=/work/build" $PWD
else
  cd /work/pyyaml/
  pip install --install-option="--prefix=/work/build" $PWD
fi

set-perms
